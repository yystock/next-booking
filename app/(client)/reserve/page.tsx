interface ReserveProps {
  searchParams: {
    [key: string]: string | string[] | undefined;
  };
}

const Reserve = async ({ searchParams }: ReserveProps) => {
  return <div></div>;
};

export default Reserve;
